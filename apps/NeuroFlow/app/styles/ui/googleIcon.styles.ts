import { StyleSheet } from 'react-native'

export const googleIconStyles = StyleSheet.create({
    googleIconWrap: {
        width: 20,
        alignItems: 'center',
    },
    googleIconText: {
        fontSize: 16,
        fontWeight: '700',
    }
})
