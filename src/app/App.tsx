import React from 'react'
import { Planner } from '@/app/components/Planner'

const Root: React.FC = () => {
    return <Planner />
}

export const App: React.FC = () => {
    return (
        <Root />
    )
}

export default App;
